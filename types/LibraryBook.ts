import { Book } from "./Book";
import { Borrowable } from "./Borrowable";

export class LibraryBook implemtnts Borr 
info: Book;

constructor (book: Book) {
    this.info = book;

}
borrow(userName: string): void {
    console.log (Книга "${ this.info.title)}
}

}